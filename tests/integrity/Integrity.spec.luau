-- Integrity.spec.luau
-- Unit tests for mechanic integrity evaluators.
-- prd_plan.md §20.1

-- TODO: Phase 5 — test cases:
--   RampJump: airborne=false → failure
--   RampJump: air_distance below threshold → failure
--   RampJump: gap_length < 2 → failure
--   RampJump: all passing → success
--   Chicane: amplitude below threshold → failure
--   CrestDip: vertical_displacement below threshold → failure
--   CrestDip: reacquired=false → failure
